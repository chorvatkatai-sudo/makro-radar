# Generierte Dateien — werden von fetch-kalender.mjs bei jedem Lauf neu gebaut
# (dashboard/data.js wird MIT versioniert, weil GitHub Pages sie braucht)
daten/naechste-woche.json
daten/marktdaten.json

# Geheimnisse — NIE veröffentlichen (Repo ist öffentlich!)
scripts/.fred-key
.fred-key
.env

# Lokale Claude-Einstellungen — nie veröffentlichen
.claude/

# Temporäres
.playwright-mcp/
*.png

# ruflo / ruvector — lokale Laufzeit-Datenbanken, nie veröffentlichen
.swarm/
ruvector.db
*.db
