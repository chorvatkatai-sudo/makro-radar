# Generierte Dateien — werden MIT versioniert (wie dashboard/data.js), damit ein
# --lokal-Build im Cloud-Sandbox (wo Feeds oft blockiert sind) die Dashboard-
# Sektionen nicht verliert: daten/naechste-woche.json + daten/marktdaten.json
# werden bei jedem erfolgreichen Online-Lauf aktualisiert und committet.

# Geheimnisse — NIE veröffentlichen (Repo ist öffentlich!)
scripts/.fred-key
.fred-key
.env

# Lokale Claude-Einstellungen — nie veröffentlichen
.claude/

# Temporäres
.playwright-mcp/
*.png

# ruflo / ruvector — lokale Laufzeit-Datenbanken, nie veröffentlichen
.swarm/
ruvector.db
*.db
